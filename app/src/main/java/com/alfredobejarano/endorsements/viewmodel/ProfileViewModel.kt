package com.alfredobejarano.endorsements.viewmodel

import android.arch.lifecycle.ViewModel

/**
 * Simple [ViewModel] class that retrieves a career profile document and
 * extract the endorsements data and parses it into a DTO object,
 */
class ProfileViewModel : ViewModel() {
}